# Copy this file to .env and fill in real values.
# .env itself is gitignored — never commit real secrets.

DB_ROOT_PASSWORD=root123
DB_NAME=portfolio_db
DB_USERNAME=portfolio_app
DB_PASSWORD=root123

# Paste your real generated secret from Step 27 — the same one you've
# been using in Run Configurations all along. Do not reuse the value
# below; it's a placeholder, not a real key.
JWT_SECRET=F5i5vISilTNLIlPkALwj4PQEfyyCSEyki28fQlX0q8PZSoXF9/TtRLl2So6XKMS1a+PbkzwYv1S9goW4KRnIFg==

ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeMe123!

CORS_ALLOWED_ORIGINS=http://localhost:3000

# This one is different from the rest — see this step's notes on why
# it stays a host-facing URL even though everything else here uses
# service names.
VITE_API_BASE_URL=http://localhost:8080/api